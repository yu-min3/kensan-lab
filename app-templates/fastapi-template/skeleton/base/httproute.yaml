apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ${{ values.name }}
  labels:
    app: ${{ values.name }}
spec:
  parentRefs:
    - name: gateway-dev  # Will be overridden by overlays
      namespace: istio-system
      kind: Gateway
  hostnames:
    - "${{ values.name }}.yu-min3.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ${{ values.name }}
          port: 8000
