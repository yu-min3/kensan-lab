# GCE Production Environment Variables
# Copy to .env and fill in values before deployment

# GCE instance external IP (set automatically by gce-deploy.sh)
GCE_IP=

# Authentication
JWT_SECRET=

# AI Service (Google Gemini)
GOOGLE_API_KEY=
GOOGLE_MODEL=gemini-2.0-flash
