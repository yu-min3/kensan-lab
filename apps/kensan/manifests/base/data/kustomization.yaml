apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kensan-db-credentials-sealed.yaml
  - kensan-minio-credentials-sealed.yaml
  - postgresql.yaml
  - minio.yaml
  - minio-init-job.yaml
  - pdb.yaml

labels:
- pairs:
    app.kubernetes.io/part-of: kensan
  includeSelectors: false
