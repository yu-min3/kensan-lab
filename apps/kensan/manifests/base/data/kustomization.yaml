apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secrets.yaml
  - postgresql.yaml
  - minio.yaml
  - minio-init-job.yaml
  - pdb.yaml

labels:
- pairs:
    app.kubernetes.io/part-of: kensan
  includeSelectors: false
