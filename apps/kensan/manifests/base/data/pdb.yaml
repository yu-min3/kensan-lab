apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: postgresql-pdb
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: postgresql
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: minio-pdb
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: minio
