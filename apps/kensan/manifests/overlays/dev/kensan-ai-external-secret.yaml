apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kensan-ai
  namespace: kensan-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: kensan-ai-credentials
    creationPolicy: Owner
  data:
    - secretKey: ANTHROPIC_API_KEY
      remoteRef: { key: kensan/dev/ai, property: ANTHROPIC_API_KEY }
    - secretKey: LAKEHOUSE_S3_ACCESS_KEY
      remoteRef: { key: kensan/dev/ai, property: LAKEHOUSE_S3_ACCESS_KEY }
    - secretKey: LAKEHOUSE_S3_SECRET_KEY
      remoteRef: { key: kensan/dev/ai, property: LAKEHOUSE_S3_SECRET_KEY }
    - secretKey: MINIO_ACCESS_KEY
      remoteRef: { key: kensan/dev/ai, property: MINIO_ACCESS_KEY }
    - secretKey: MINIO_SECRET_KEY
      remoteRef: { key: kensan/dev/ai, property: MINIO_SECRET_KEY }
    - secretKey: OPENAI_API_KEY
      remoteRef: { key: kensan/dev/ai, property: OPENAI_API_KEY }
    - secretKey: POLARIS_CREDENTIAL
      remoteRef: { key: kensan/dev/ai, property: POLARIS_CREDENTIAL }
