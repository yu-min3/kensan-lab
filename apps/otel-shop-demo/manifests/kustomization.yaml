apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app-dev

resources:
  - postgresql-statefulset.yaml
  - backend-deployment.yaml
  - frontend-deployment.yaml
  - httproute.yaml

commonLabels:
  app.kubernetes.io/name: otel-shop-demo
  app.kubernetes.io/component: demo
