# This is a placeholder for the sealed secret
# To generate the actual sealed secret:
# 1. Edit temp/backstage-secret-raw.yaml with real credentials
# 2. Run: kubeseal --format=yaml < temp/backstage-secret-raw.yaml > base-infra/backstage/prod1/backstage-secret.yaml
# 3. Commit the sealed secret to git
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: backstage-secret
  namespace: backstage
spec:
  encryptedData:
    POSTGRES_PASSWORD: PLACEHOLDER_TO_BE_SEALED
    POSTGRES_USER: PLACEHOLDER_TO_BE_SEALED
    GITHUB_TOKEN: PLACEHOLDER_TO_BE_SEALED
  template:
    metadata:
      name: backstage-secret
      namespace: backstage
    type: Opaque
