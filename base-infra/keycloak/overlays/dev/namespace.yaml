apiVersion: v1
kind: Namespace
metadata:
  name: platform-auth-dev
  labels:
    name: platform-auth-dev
    environment: development
    platform: infrastructure  # Allow access from gateway-platform
