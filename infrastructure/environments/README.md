# Environments

This directory contains the configuration for different environments.

## Application Environments

- `app-dev`: Development environment for applications.
- `app-prod`: Production environment for applications.

## System Environments

- `system-infra`: Contains configurations for system-level infrastructure components that are cluster-wide, such as labels for the `kube-system` namespace.
