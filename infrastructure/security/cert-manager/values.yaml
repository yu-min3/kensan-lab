# Chart config: infrastructure/gitops/argocd/applications/security/cert-manager/app.yaml
# Version: v1.19.1
#
# TLS certificate management with Let's Encrypt

# Install CRDs with the chart (required for Certificate, ClusterIssuer, etc.)
crds:
  enabled: true
