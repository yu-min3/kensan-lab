apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: network-policy
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-4"  # Cluster-wide network policy: deploy before workloads
    argocd.argoproj.io/sync-options: Prune=false  # disaster recovery: CCNP default-deny + per-ns allow を保護 (一括 prune で全 mesh ns が黙る)
  # NOTE: resources-finalizer 不在 — Application 削除で CCNP 蒸発 → mesh 通信全滅を防ぐ
spec:
  project: platform-project

  source:
    repoURL: https://github.com/yu-min3/kensan-lab
    targetRevision: main
    path: kubernetes/network/network-policy

  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
