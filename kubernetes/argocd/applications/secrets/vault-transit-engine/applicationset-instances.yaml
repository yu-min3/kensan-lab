## vault-transit-engine の per-consumer ArgoCD app 自動生成
##
## convention: <owner-dir>/platform-values/vault-transit/<instance>.yaml にファイル置く
## ⇒ AppSet が ** recursive glob で自動 discover、name を path から inject
##
## 将来 AD が apps/<name>/platform-values/vault-transit/<inst>.yaml に置いても
## AppSet 編集ゼロで pickup される (vault-database-engine と同パターン)。
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vault-transit-engine-instances
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/yu-min3/kensan-lab
        revision: HEAD
        files:
          - path: "**/platform-values/vault-transit/*.yaml"
  template:
    metadata:
      ## name = "vault-transit-<filename-basename>" (拡張子を除いたもの)
      name: 'vault-transit-{{ trimSuffix ".yaml" .path.filename }}'
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: "9"  # vault-transit-engine-shared (8) の後
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: platform-project
      sources:
        - repoURL: https://github.com/yu-min3/kensan-lab
          targetRevision: main
          path: kubernetes/secrets/vault-transit-engine/chart
          helm:
            releaseName: 'transit-{{ trimSuffix ".yaml" .path.filename }}'
            ## name は path から自動 inject (AD は values file に書かない)
            parameters:
              - name: name
                value: 'transit-{{ trimSuffix ".yaml" .path.filename }}'
            valueFiles:
              - $values/{{ .path.path }}/{{ .path.filename }}
        - repoURL: https://github.com/yu-min3/kensan-lab
          targetRevision: main
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: vault
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
          - ServerSideApply=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
